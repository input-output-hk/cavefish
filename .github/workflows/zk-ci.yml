name: ZK prototype CI

on:
  pull_request:
    paths:
      - '.github/workflows/zk-ci.yml'
      - 'zk-wbps/**'
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/zk-ci.yml'
      - 'zk-wbps/**'

jobs:
  build-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: zk-wbps

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Node is needed by snarkjs + the circom wasm runner
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'   # 20+ is fine for snarkjs

      # circom is built with Rust
      - name: Setup Rust (for circom)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo (speeds up circom build)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cache/sccache
          key: cargo-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-${{ runner.os }}-

      - name: Install circom & snarkjs
        run: |
          cargo install --git https://github.com/iden3/circom --locked --force
          npm i -g snarkjs@^0.7.0
          circom --version
          snarkjs -v || true

      # Your circuit file includes ../vendor/circomlib/... when compiled
      # from zk-wbps/circuits, so we place the dependency under zk-wbps/vendor/.
      - name: Fetch circomlib (vendor)
        run: |
          mkdir -p vendor
          if [ ! -d vendor/circomlib ]; then
            git clone --depth=1 https://github.com/iden3/circomlib.git vendor/circomlib
          fi

      - name: Build with Makefile
        run: |
          make
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: zk-wbps-build
          path: |
            zk-wbps/build/wbps_cardano/*.r1cs
            zk-wbps/build/wbps_cardano/*.sym
            zk-wbps/build/wbps_cardano/wbps_cardano_js/**
            zk-wbps/build/wbps_cardano/*.wtns
            zk-wbps/build/wbps_cardano/*proof.json
            zk-wbps/build/wbps_cardano/*public.json
            zk-wbps/build/wbps_cardano/*verifkey.json
          if-no-files-found: warn
